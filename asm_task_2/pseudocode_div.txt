// a / b = q, q - ?

a_scaled = a_raw * 4
lo = 0
hi = a_scaled

while (hi - lo) > 1:
    mid = (lo + hi) / 2
    product = mid * b_raw

    if product == a_scaled:
        q = mid
        выход
    else if product < a_scaled:
        lo = mid
    else:
        hi = mid

q = lo

# Репозиторий с заданиями по курсу "Эффективная реалиизация AI"

- Эссе находится в `./computational_essay`. Там лежит сам ноутбук и статья, на основе которой делаллось эссе.
- Трансформер лежит в `./small_transformer`. Python модель лежит в `copy_transformer.ipynb`, код ассемблера в `main.asm`, макрдаун рабочего журнала: `Small_transformer.md` . Ключевые моменты в журнале идут после абзаца "Реализация копирующего трансформера на sim8".